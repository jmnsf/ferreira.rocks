{
  "name": "ferreira-rocks",
  "description": "Personal website",
  "dependencies": {
    "@webcomponents/webcomponentsjs": "^2.0.0"
  },
  "devDependencies": { },
  "scripts": {
    "build": "cp -r -t dist *.html node_modules css icons photos images src manifest.json browserconfig.xml favicon.ico",
    "deploy": "yarn build && rsync -rmz --progress --delete-delay dist/. proxy.jmnsf.com:/data/www/ferreira.rocks/",
    "serve": "ruby -run -ehttpd . -p8000",
    "start": "yarn serve"
  }
}
